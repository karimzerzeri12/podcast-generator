# Optional. Leave unset for normal use — `npm run dev` uses http://localhost:8000 and a
# bundled/deployed build uses its own origin. Only set this to point the frontend at a
# different backend host:
# VITE_API_BASE_URL=http://localhost:8000
